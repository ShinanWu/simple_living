apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-domain
  namespace: simple-living
  labels:
    app: tracking-domain
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: tracking-domain
  template:
    metadata:
      labels:
        app: tracking-domain
    spec:
      containers:
        - name: tracking-domain
          image: registry.example.com/simple-living/tracking-domain:__IMAGE_TAG__
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9105
              name: grpc
          envFrom:
            - configMapRef:
                name: common-env
            - secretRef:
                name: app-secrets
          readinessProbe:
            # Placeholder for gRPC health endpoint.
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            # Placeholder for gRPC health endpoint.
            tcpSocket:
              port: grpc
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
