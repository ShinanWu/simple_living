package com.simpleliving.android.model

enum class Theme(val label: String, val apiValue: String) {
    Clothing(label = "衣", apiValue = "clothing"),
    Food(label = "食", apiValue = "food"),
    Housing(label = "住", apiValue = "housing"),
    Transport(label = "行", apiValue = "transport"),
}
