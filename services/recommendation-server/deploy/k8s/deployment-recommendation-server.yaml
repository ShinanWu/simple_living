apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-server
  namespace: simple-living
  labels:
    app: recommendation-server
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: recommendation-server
  template:
    metadata:
      labels:
        app: recommendation-server
    spec:
      containers:
        - name: recommendation-server
          image: registry.example.com/simple-living/recommendation-server:__IMAGE_TAG__
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9103
              name: grpc
          envFrom:
            - configMapRef:
                name: common-env
            - secretRef:
                name: app-secrets
          readinessProbe:
            # Placeholder for gRPC health endpoint.
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            # Placeholder for gRPC health endpoint.
            tcpSocket:
              port: grpc
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
